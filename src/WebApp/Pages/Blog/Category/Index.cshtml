@page "/cat/{slug?}"
@using WebApp.Extensions
@model WebApp.Pages.Blog.Category.IndexModel
@{
    ViewData["Title"] = Model.IsDirectoryPage ? "分类" : $"{Model.CategoryName} - 博客分类";
    ViewData["Description"] = Model.IsDirectoryPage
        ? "浏览码墙全部文章分类。"
        : (string.IsNullOrWhiteSpace(Model.CategoryMemo) ? $"查看 {Model.CategoryName} 分类下的技术文章。" : Model.CategoryMemo);
    var categorySlug = Model.CurrentSlug ?? string.Empty;
    var categories = Model.Categories
        .Where(item => !string.Equals(item.Slug, "default", StringComparison.OrdinalIgnoreCase))
        .ToList();
}

<section class="page-shell">
    <div class="container">
        <div class="page-banner">
            <div>
                <span class="eyebrow">Category</span>
                <h1 class="page-title">@(Model.IsDirectoryPage ? "全部分类" : Model.CategoryName)</h1>
                <p class="page-description">@(string.IsNullOrWhiteSpace(Model.CategoryMemo) ? "按分类浏览站内文章内容。" : Model.CategoryMemo)</p>
            </div>
            @if (!Model.IsDirectoryPage)
            {
                <div class="page-banner__stats">
                    <div class="stat-chip">
                        <strong>@Model.Total</strong>
                        <span>篇文章</span>
                    </div>
                    <div class="stat-chip">
                        <strong>@Model.PageIndex</strong>
                        <span>当前页</span>
                    </div>
                </div>
            }
        </div>

        @if (Model.IsDirectoryPage)
        {
            <div class="surface-panel directory-panel">
                @if (!categories.Any())
                {
                    <div class="empty-state">
                        <h2>暂未配置分类</h2>
                        <p>后续会在这里展示全部文章分类。</p>
                    </div>
                }
                else
                {
                    <div class="directory-grid">
                        @foreach (var category in categories)
                        {
                            <a href="@ConstantUtil.GetCategoryUrl(category.Slug!)" class="directory-card">
                                <strong>@category.Name</strong>
                                @if (!string.IsNullOrWhiteSpace(category.Memo))
                                {
                                    <p>@category.Memo</p>
                                }
                            </a>
                        }
                    </div>
                }
            </div>
        }
        else
        {
            <div class="content-grid">
                <div class="content-primary">
                    @if (!Model.Posts.Any())
                    {
                        <div class="empty-state">
                            <h2>这个分类下还没有文章</h2>
                            <p>可以先去首页或博客列表看看其他内容。</p>
                        </div>
                    }
                    else
                    {
                        <div class="post-list">
                            @foreach (var post in Model.Posts)
                            {
                                var postUrl = ConstantUtil.GetPostUrl(post);
                                var hasCover = !string.IsNullOrWhiteSpace(post.Cover);
                                <article class="post-list-card @(hasCover ? string.Empty : "post-list-card--text-only")">
                                    @if (hasCover)
                                    {
                                        <a href="@postUrl" class="post-list-card__cover">
                                            <img src="@post.Cover" alt="@post.Title" class="cover-media" loading="lazy" decoding="async" />
                                        </a>
                                    }
                                    <div class="post-list-card__body">
                                        <h2 class="post-list-card__title"><a href="@postUrl">@post.Title</a></h2>
                                        @if (!string.IsNullOrWhiteSpace(post.Description))
                                        {
                                            <p class="post-list-card__excerpt">@post.Description</p>
                                        }
                                        <div class="meta-line">
                                            <span><i class="far fa-user"></i>@(string.IsNullOrWhiteSpace(post.Author) ? Model.Owner : post.Author)</span>
                                            <span><i class="far fa-calendar"></i>@I18n.FormatDateTime(post.Lastmod ?? post.Date)</span>
                                        </div>
                                    </div>
                                </article>
                            }
                        </div>
                    }

                    @if (Model.TotalPages > 1)
                    {
                        <nav class="pagination-shell" aria-label="分类分页">
                            <p class="pagination-summary">第 @Model.PageIndex / @Model.TotalPages 页，共 @Model.Total 篇文章</p>
                            <ul class="pagination justify-content-center pagination-list">
                                <li class="page-item @(Model.PageIndex <= 1 ? "disabled" : "")">
                                    <a class="page-link" href="@ConstantUtil.GetCategoryUrl(categorySlug)?pageIndex=@(Model.PageIndex - 1)">上一页</a>
                                </li>
                                @{
                                    var startPage = Math.Max(1, Model.PageIndex - 2);
                                    var endPage = Math.Min(Model.TotalPages, Model.PageIndex + 2);
                                    if (endPage - startPage < 4)
                                    {
                                        if (startPage == 1)
                                        {
                                            endPage = Math.Min(Model.TotalPages, startPage + 4);
                                        }
                                        else if (endPage == Model.TotalPages)
                                        {
                                            startPage = Math.Max(1, endPage - 4);
                                        }
                                    }
                                }
                                @if (startPage > 1)
                                {
                                    <li class="page-item">
                                        <a class="page-link" href="@ConstantUtil.GetCategoryUrl(categorySlug)?pageIndex=1">1</a>
                                    </li>
                                    @if (startPage > 2)
                                    {
                                        <li class="page-item disabled">
                                            <span class="page-link">...</span>
                                        </li>
                                    }
                                }
                                @for (var i = startPage; i <= endPage; i++)
                                {
                                    <li class="page-item @(Model.PageIndex == i ? "active" : "")">
                                        <a class="page-link" href="@ConstantUtil.GetCategoryUrl(categorySlug)?pageIndex=@i">@i</a>
                                    </li>
                                }
                                @if (endPage < Model.TotalPages)
                                {
                                    @if (endPage < Model.TotalPages - 1)
                                    {
                                        <li class="page-item disabled">
                                            <span class="page-link">...</span>
                                        </li>
                                    }
                                    <li class="page-item">
                                        <a class="page-link" href="@ConstantUtil.GetCategoryUrl(categorySlug)?pageIndex=@Model.TotalPages">@Model.TotalPages</a>
                                    </li>
                                }
                                <li class="page-item @(Model.PageIndex >= Model.TotalPages ? "disabled" : "")">
                                    <a class="page-link" href="@ConstantUtil.GetCategoryUrl(categorySlug)?pageIndex=@(Model.PageIndex + 1)">下一页</a>
                                </li>
                            </ul>
                        </nav>
                    }
                </div>

                <aside class="content-sidebar">
                    <div class="surface-panel sidebar-panel sticky-panel">
                        <h2 class="sidebar-title">全部分类</h2>
                        <div class="sidebar-link-list">
                            @foreach (var category in categories)
                            {
                                <a href="@ConstantUtil.GetCategoryUrl(category.Slug!)" class="sidebar-link @(category.Slug == categorySlug ? "is-current" : string.Empty)">
                                    <span>@category.Name</span>
                                    <i class="fas fa-angle-right"></i>
                                </a>
                            }
                        </div>
                    </div>
                </aside>
            </div>
        }
    </div>
</section>
