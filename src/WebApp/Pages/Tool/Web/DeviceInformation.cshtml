@page "/tool/device-information"
@{
    ViewData["Title"] = "设备信息";
    ViewData["Description"] = "查看当前浏览器、屏幕、语言、在线状态和视口等设备信息。";
    ViewData["CanonicalPath"] = "/tool/device-information";
    var banner = new WebApp.Models.PageBannerViewModel
    {
        Eyebrow = "网页工具",
        Title = "设备信息",
        Description = "查看当前浏览器、屏幕、语言、在线状态和视口等设备信息。",
        MetaItems = ["Razor Pages", "原生工具", "device-information"]
    };
}

<section class="page-shell">
    <div class="container">
        <partial name="_PageBanner" model="banner" />

        <div class="tool-detail-nav">
            <a class="inline-link" href="/tool">
                <i class="fa-solid fa-arrow-left" aria-hidden="true"></i>
                返回工具箱
            </a>
        </div>

        <article class="surface-panel native-tool-host">
            <div class="native-tool-panel__head">
                <div>
                    <span class="card-kicker">Razor Pages 原生工具</span>
                    <h2 class="surface-title">设备信息</h2>
                </div>
                <button type="button" class="btn btn-ghost btn-sm" onclick="copyOutput()">
                    <i class="fa-regular fa-copy" aria-hidden="true"></i> 复制结果
                </button>
            </div>
            <div class="native-tool-actions"><button type="button" class="btn btn-primary" onclick="runTool()">刷新</button></div><textarea class="form-control native-textarea" id="output" rows="14" readonly></textarea>
        </article>
    </div>
</section>

@section Scripts {
<script>

function $(id) { return document.getElementById(id); }
function setOutput(value) { const output = $('output'); if (output) output.value = value ?? ''; }
function showError(error) { setOutput(error?.message ?? String(error)); }
async function copyOutput() {
    const output = $('output');
    if (!output) return;
    try { await navigator.clipboard.writeText(output.value || output.textContent || ''); }
    catch { output.select?.(); document.execCommand('copy'); }
}
function escapeHtml(value) {
    return String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#39;');
}
function loadScript(src, globalName) {
    if (globalName && window[globalName]) return Promise.resolve(window[globalName]);
    return new Promise((resolve, reject) => {
        const script = document.createElement('script');
        script.src = src;
        script.async = true;
        script.onload = () => resolve(globalName ? window[globalName] : true);
        script.onerror = () => reject(new Error('资源加载失败：' + src));
        document.head.appendChild(script);
    });
}
function runTool(){setOutput(JSON.stringify({UserAgent:navigator.userAgent,平台:navigator.platform,语言:navigator.language,Cookie启用:navigator.cookieEnabled,在线状态:navigator.onLine,屏幕:{width:screen.width,height:screen.height,colorDepth:screen.colorDepth},视口:{width:innerWidth,height:innerHeight,devicePixelRatio}},null,2));}document.addEventListener('DOMContentLoaded',runTool);
</script>
}
