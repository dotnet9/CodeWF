@page "/tool/keycode-info"
@{
    ViewData["Title"] = "键盘按键码查询";
    ViewData["Description"] = "按下任意键后显示 key、code、keyCode 和修饰键状态。";
    ViewData["CanonicalPath"] = "/tool/keycode-info";
    var banner = new WebApp.Models.PageBannerViewModel
    {
        Eyebrow = "网页工具",
        Title = "键盘按键码查询",
        Description = "按下任意键后显示 key、code、keyCode 和修饰键状态。",
        MetaItems = ["Razor Pages", "原生工具", "keycode-info"]
    };
}

<section class="page-shell">
    <div class="container">
        <partial name="_PageBanner" model="banner" />

        <div class="tool-detail-nav">
            <a class="inline-link" href="/tool">
                <i class="fa-solid fa-arrow-left" aria-hidden="true"></i>
                返回工具箱
            </a>
        </div>

        <article class="surface-panel native-tool-host">
            <div class="native-tool-panel__head">
                <div>
                    <span class="card-kicker">Razor Pages 原生工具</span>
                    <h2 class="surface-title">键盘按键码查询</h2>
                </div>
                <button type="button" class="btn btn-ghost btn-sm" onclick="copyOutput()">
                    <i class="fa-regular fa-copy" aria-hidden="true"></i> 复制结果
                </button>
            </div>
            <div class="native-key-target" tabindex="0" id="target">聚焦这里并按下任意键</div><textarea class="form-control native-textarea" id="output" rows="8" readonly></textarea>
        </article>
    </div>
</section>

@section Scripts {
<script>

function $(id) { return document.getElementById(id); }
function setOutput(value) { const output = $('output'); if (output) output.value = value ?? ''; }
function showError(error) { setOutput(error?.message ?? String(error)); }
async function copyOutput() {
    const output = $('output');
    if (!output) return;
    try { await navigator.clipboard.writeText(output.value || output.textContent || ''); }
    catch { output.select?.(); document.execCommand('copy'); }
}
function escapeHtml(value) {
    return String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#39;');
}
function loadScript(src, globalName) {
    if (globalName && window[globalName]) return Promise.resolve(window[globalName]);
    return new Promise((resolve, reject) => {
        const script = document.createElement('script');
        script.src = src;
        script.async = true;
        script.onload = () => resolve(globalName ? window[globalName] : true);
        script.onerror = () => reject(new Error('资源加载失败：' + src));
        document.head.appendChild(script);
    });
}
document.addEventListener('DOMContentLoaded', () => $('target').addEventListener('keydown', event => { event.preventDefault(); setOutput(JSON.stringify({key:event.key,code:event.code,keyCode:event.keyCode,location:event.location,altKey:event.altKey,ctrlKey:event.ctrlKey,metaKey:event.metaKey,shiftKey:event.shiftKey}, null, 2)); }));
</script>
}
