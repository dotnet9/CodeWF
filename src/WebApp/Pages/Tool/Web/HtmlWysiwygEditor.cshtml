@page "/tool/html-wysiwyg-editor"
@{
    ViewData["Title"] = "HTML 所见即所得编辑器";
    ViewData["Description"] = "在线编辑富文本内容，并实时查看生成的 HTML。";
    ViewData["CanonicalPath"] = "/tool/html-wysiwyg-editor";
    var banner = new WebApp.Models.PageBannerViewModel
    {
        Eyebrow = "网页工具",
        Title = "HTML 所见即所得编辑器",
        Description = "在线编辑富文本内容，并实时查看生成的 HTML。",
        MetaItems = ["Razor Pages", "原生工具", "html-wysiwyg-editor"]
    };
}

<section class="page-shell">
    <div class="container">
        <partial name="_PageBanner" model="banner" />

        <div class="tool-detail-nav">
            <a class="inline-link" href="/tool">
                <i class="fa-solid fa-arrow-left" aria-hidden="true"></i>
                返回工具箱
            </a>
        </div>

        <article class="surface-panel native-tool-host">
            <div class="native-tool-panel__head">
                <div>
                    <span class="card-kicker">Razor Pages 原生工具</span>
                    <h2 class="surface-title">HTML 所见即所得编辑器</h2>
                </div>
                <button type="button" class="btn btn-ghost btn-sm" onclick="copyOutput()">
                    <i class="fa-regular fa-copy" aria-hidden="true"></i> 复制结果
                </button>
            </div>
            <div class="native-tool-actions"><button type="button" class="btn btn-ghost" data-cmd="bold">B</button><button type="button" class="btn btn-ghost" data-cmd="italic">I</button><button type="button" class="btn btn-ghost" data-cmd="insertUnorderedList">列表</button><button type="button" class="btn btn-ghost" id="heading">标题</button></div><div class="native-editor" id="editor" contenteditable="true"><h2>Hello CodeWF</h2><p>编辑这里的富文本内容。</p></div><label class="native-field native-field--full"><span>HTML</span><textarea class="form-control native-textarea native-textarea--output" id="output" rows="10" readonly></textarea></label>
        </article>
    </div>
</section>

@section Scripts {
<script>

function $(id) { return document.getElementById(id); }
function setOutput(value) { const output = $('output'); if (output) output.value = value ?? ''; else if (output) output.textContent = value ?? ''; }
function showError(error) { setOutput(error?.message ?? String(error)); }
async function copyOutput() {
    const output = $('output');
    if (!output) return;
    try { await navigator.clipboard.writeText(output.value || output.textContent || ''); }
    catch { output.select?.(); document.execCommand('copy'); }
}
function escapeHtml(value) {
    return String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#39;');
}
function loadScript(src, globalName) {
    if (globalName && window[globalName]) return Promise.resolve(window[globalName]);
    return new Promise((resolve, reject) => {
        const script = document.createElement('script');
        script.src = src;
        script.async = true;
        script.onload = () => resolve(globalName ? window[globalName] : true);
        script.onerror = () => reject(new Error('资源加载失败：' + src));
        document.head.appendChild(script);
    });
}
const moduleCache = new Map();
function loadModule(src) {
    if (!moduleCache.has(src)) moduleCache.set(src, import(src));
    return moduleCache.get(src);
}

function sync(){setOutput($('editor').innerHTML);} function runTool(){sync();}
document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('input, textarea, select').forEach(el => {
        el.addEventListener('input', () => runTool?.());
        el.addEventListener('change', () => runTool?.());
    });
    document.querySelectorAll('[data-cmd]').forEach(b=>b.addEventListener('click',()=>{document.execCommand(b.dataset.cmd,false,null); sync();})); $('heading').addEventListener('click',()=>{document.execCommand('formatBlock',false,'h2'); sync();}); $('editor').addEventListener('input', sync); sync();
});
</script>
}
